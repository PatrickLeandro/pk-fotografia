import {failure,isAdmin,profile,runtime} from '@/lib/server';
export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){try{const{id}=await params;const p=await profile();if(![p.logo,p.logoDark].includes(`/api/logo/${id}`)&&!await isAdmin())return new Response('Não encontrada',{status:404});const obj=await runtime.BUCKET.get(`brand/${id}.webp`);if(!obj)return new Response('Não encontrada',{status:404});return new Response(obj.body,{headers:{'Content-Type':'image/webp','X-Content-Type-Options':'nosniff','Cache-Control':'private, no-store'}})}catch(e){return failure(e)}}
